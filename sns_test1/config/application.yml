#端口
server:
  port: 8081
  
#mysql jdbc
spring:
  datasource:


    
    #druid配置
    druid:
      aop-patterns: hello.test.*
      stat-view-servlet:
        login-username: admin
        login-password: 123
  
#mybatis加载
mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: hello.test.entity
  #下划线转驼峰命名
  configuration:
    map-underscore-to-camel-case: true
    

    
